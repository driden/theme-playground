{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Environment setup & latest features
    "lib": ["ESNext", "DOM"],
    "jsx": "react-jsx",
    "allowJs": true,
    "types": ["bun", "react"],

    "paths": {
      "@/*": ["./src/*"]
    },
  },
}
